apiVersion: v1
kind: Service

metadata:
  name: cloudforge-backend-service

spec:
  selector:
    app: cloudforge-backend

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000

  type: NodePort
