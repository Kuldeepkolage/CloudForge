apiVersion: v1
kind: Service

metadata:
  name: cloudforge-frontend-service

spec:
  selector:
    app: cloudforge-frontend

  ports:
    - port: 5173
      targetPort: 5173
      nodePort: 30173

  type: NodePort
